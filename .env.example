# Database (Optional - only needed for admin features)
DATABASE_URL="postgresql://user:password@localhost:5432/solarautopilot"

# NextAuth (Optional - only needed for admin features)
NEXTAUTH_URL="http://192.168.160.98"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Email (Optional - only needed for contact form)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"
SMTP_FROM="noreply@solarautopilot.com"

# Site Configuration
NEXT_PUBLIC_SITE_URL="http://192.168.160.98"
